import {Typography} from "antd";

const {Title} = Typography;

export const Header = () => {
  return (
    <section className="headerRoot">
      <Title className="title">Weather Forcast</Title>
    </section>
  );
};
